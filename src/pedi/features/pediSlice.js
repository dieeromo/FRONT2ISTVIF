
import { createSlice ,PayloadAction} from '@reduxjs/toolkit'

const initialState = {
  pedi: null,
}

const pediSlice = createSlice({
  name: 'pedi',
  initialState,
  reducers: {

  },
})


export default pediSlice.reducer
